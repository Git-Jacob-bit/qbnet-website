version: '3.8'

services:
  qbnet-web:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: qbnet_nextjs
    ports:
      - "3000:3000"
    env_file:
     - .env.local
    restart: always
    environment:
      - NODE_ENV=production
